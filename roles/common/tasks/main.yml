---
- block:
  - import_tasks: prepare.yml
    tags:
      - prepare
      - common.prepare
  - import_tasks: install.yml
    tags:
      - install
      - common.install
  - import_tasks: configure.yml
    tags:
      - configure
      - common.configure
  tags:
    - common
